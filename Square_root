//square root

#include <stdio.h>
#include <math.h>

float mySqrt() {
    float x;
    scanf("%f", &x);
    float n = sqrt(x);
    printf("%f\n", n);
    return n;
}

int main() {
    float result = mySqrt();
    float result1 = result * result;
    printf("%f", result1);
    return 0;
}
